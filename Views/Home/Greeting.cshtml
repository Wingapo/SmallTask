@{
    ViewData["Title"] = $"Hello {@ViewBag.Name}";
}
<div class="mt-5">
    <h1 class="display-4">Hello, @ViewBag.Name!</h1>
    <a class="btn btn-primary" asp-controller="Home" asp-action="Index">Back</a>
</div>